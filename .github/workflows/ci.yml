name: ci

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  wfc:
    uses: lornu-ai/ci-checks/.github/workflows/wfc-gha.yml@main
    secrets: inherit
